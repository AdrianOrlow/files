import React from 'react';

import { Container } from './LoginStyle';

const Login: React.FC = () => (
  <main>
    <Container>
      Login in progress
    </Container>
  </main>
);

export default Login;
